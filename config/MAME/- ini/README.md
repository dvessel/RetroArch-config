This directory softlinked from `RetroArch/system/mame/ini` to track changes. Other directories linked to here are from the homebrew install of MAME to provide examples.

Ensure `mame_read_config = "enabled"` is set for the core setting.

